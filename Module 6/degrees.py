import math

# Prompt the user to enter a radian value
radian_value = float(input("Enter a radian value: "))

# Convert radians to degrees using the math module's degrees function
degrees = math.degrees(radian_value)

# Print the converted degrees value
print("Degrees:", degrees)